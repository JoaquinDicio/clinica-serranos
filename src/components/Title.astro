---
const { title, extraClasses, lightMode } = Astro.props;
---

<div class={extraClasses}>
  <h2 class={"font-[600] text-[var(--main-color)] text-center pb-3 text-xl"}>
    {title}
  </h2>
  <div
    class={`rounded-full w-[55px] h-[7px] ${lightMode ? "bg-white" : "bg-[#1E79FE]"} mx-auto`}
  >
  </div>
</div>
